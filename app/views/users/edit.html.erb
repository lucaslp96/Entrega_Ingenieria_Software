
<%= debug(params) %>

<div class="container">
<%= form_for @user do |f|%>
<div class="form-group">
  <%= f.label :Nombre %>
  <%= f.text_field :name, class:'form-control', required: true %>
</div>
    <div class="form-group" id:"eleccionUniversidad">
      <label for="eleccionUniversidad">Elegí tu universidad :</label>
	<%= f.select :university_id,  University.all.collect {|x| [x.name, x.id]}, {include_blank: false}, class: "form-control" %>
  <%unless @user.errors[:universiy_id].empty?%>
    <div class="alert alert-danger">
      <%= @user.errors[:university_id].join(", ") %>
    </div>
  <% end %>
</div>
<div>
    <%= f.submit 'Guardar',class:'btn btn-primary',role: 'button' %>
</div>
<% end %>
</div>
