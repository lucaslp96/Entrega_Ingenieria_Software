<!--
<br>

<h3>

<div class="container">
<div class="UserName">


<p>
<%= current_user.name %>
</p>
</div>
<hr>
<p>
Email: <%= current_user.email %>
</p>
<hr>
<p>
Universidad: <% if (current_user.university_id != nil) %> <%= current_user.university_id %> <% else %> Todavia no cuenta con una universidad asignada. <% end %>
</p>
<hr>
<p>
Sus preguntas:
</p>
<% @questions.each do |preg| %>

        <li>-<%= link_to preg.title, preg %> </li>

<% end %>

</h3>

</div>
-->

<link href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css" rel="stylesheet">
<div class="container">
	<div class="row">
		<div class="col-md-offset-2 col-md-8 col-lg-offset-3 col-lg-6">
    	 <div class="well profile">
            <div class="col-sm-12">
                <div class="col-xs-12 ">
                    <h2><%= current_user.name %></h2>
                    <p><strong>Email: </strong> <%= current_user.email %> </p>
                    <p><strong>Universidad: </strong> <% if (current_user.university_id != nil) %> <%= current_user.university_id %> <% else %> Todavia no cuenta con una universidad asignada. <% end %> </p>
                    <p><strong>Etiquetas: </strong>
                        <span class="tags">Informatica</span>
                        <span class="tags">Tenis</span>
                        <span class="tags">Futbol</span>
                        <span class="tags">Ajedrez</span>
                        <span class="tags">HTML</span>
                    </p>
                      <hr>
                      <%= link_to 'Editar', '', class: 'btn btn-primary btn-lg btn-prymary-spacing',role: 'button' %>
                </div>


            </div>

    	 </div>
		</div>
	</div>
</div>
