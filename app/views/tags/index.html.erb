<br><br><center>

<% if user_signed_in? %>
	<br>
    	<center>
    		<%= link_to "Crear categoría", new_tag_path %>
    	</center>
	<br>
<% end %>

<div class="container">
	<% @tags.each do |tag| %>
  		<div class="row">
  			<div class="col-sm-4">
  			</div>
    		<div class="col-sm-2">
				<%= tag.content %>
    		</div>
    		<div class="col-sm-2">
				usada <%= tag.usos %> veces
    		</div>
  		</div>
	<% end %>
</div>

</center><br><br><br>